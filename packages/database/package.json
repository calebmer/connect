{
  "name": "@connect/database",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "drop": "node scripts/drop.js",
    "reset": "yarn drop && yarn migrate"
  },
  "dependencies": {
    "chalk": "^2.4.2",
    "pg": "^7.8.1"
  }
}
