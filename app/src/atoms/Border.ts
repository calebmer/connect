// NOTE: We don’t have a `width0` in our border width scale. If we did it would
// be `StyleSheet.hairlineWidth`. However, on web `StyleSheet.hairlineWidth` is
// 1px which looks significantly different from `StyleSheet.hairlineWidth` on a
// mobile device. We’d prefer to not have significant differences like this in
// our scale.

export const width1 = 1;
export const width2 = 3;
export const width3 = 8;

export const radius0 = 4;
export const radius1 = 8;
export const radius2 = 16;
